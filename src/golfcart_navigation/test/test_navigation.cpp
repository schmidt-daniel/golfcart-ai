#include "gtest/gtest.h"

namespace golfcart
{

// Placeholder test for the navigation node.
// The navigation_node is a ROS bridge; its logic is validated via integration
// tests. This test ensures the test target builds and runs.
TEST(Navigation, Smoke)
{
  EXPECT_TRUE(true);
}

}  // namespace golfcart